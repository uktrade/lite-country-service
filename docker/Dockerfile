FROM java:8

ARG SERVICE_NAME
ARG JAR_FILE
ARG CONFIG_FILE

COPY docker/$JAR_FILE /opt/$SERVICE_NAME/

COPY docker/$CONFIG_FILE /opt/$SERVICE_NAME/

EXPOSE 9090

WORKDIR /opt/$SERVICE_NAME

ENV JAR_FILE $JAR_FILE
ENV CONFIG_FILE $CONFIG_FILE

CMD java "-jar" $JAR_FILE "server" $CONFIG_FILE



