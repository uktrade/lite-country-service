cacheExpirySeconds: 86400

countryListCacheJobCron: 0 0 0 * * ? # run at midnight everyday

countryListCacheRetryJobCron: 0 0/15 * * * ? # retry every 15 mins

soapAction: http://www.fivium.co.uk/fox/webservices/ispire/SPIRE_COUNTRIES/getCountries

soapUrl: https://spiredevalpha.fivium.co.uk/spirefox4dev/fox/ispire/SPIRE_COUNTRIES

soapNamespace: http://www.fivium.co.uk/fox/webservices/ispire/SPIRE_COUNTRIES/getCountries

spireCredentials: bisdev.api@test.com:dev

server:
  type: default
  applicationConnectors:
    - type: http
      port: 8090
  adminConnectors:
    - type: http
      port: 8091

logging:

  level: INFO

  appenders:
    - type: file
      # The file to which current statements will be logged.
      currentLogFilename: ./logs/lite-country-service.log

      # When the log file rotates, the archived log will be renamed to this and gzipped. The
      # %d is replaced with the previous day (yyyy-MM-dd). Custom rolling windows can be created
      # by passing a SimpleDateFormat-compatible format as an argument: "%d{yyyy-MM-dd-hh}".
      archivedLogFilenamePattern: ./logs/lite-country-service-%d.log.gz

      # The number of archived files to keep.
      archivedFileCount: 5

      # The timezone used to format dates. HINT: USE THE DEFAULT, UTC.
      timeZone: UTC