cacheExpirySeconds: 1440
countryListCacheJobCron: 0 0 0 * * ? # run at midnight everyday
soapUrl: http://localhost:9000/spirefox4dev/fox/ispire/SPIRE_COUNTRIES
spireCredentials: PUT_CREDENTIALS_HERE

server:
  type: default
  applicationConnectors:
    - type: http
      port: 8090

logging:
  level: INFO
  appenders:
    - type: console